{
  "plan_source": ".claude/plans/tileset-room-generation.md",
  "created_at": "2026-03-13T00:00:00Z",
  "stories": [
    {
      "id": "story-1",
      "title": "Floor and wall tiles → Sprite2D",
      "description": "File: scripts/room_scene.gd\n\n1. At the top of the file (after 'class_name RoomScene'), add preloaded texture constants:\n   const TEX_FLOORS := preload(\"res://assets/tilesets/_Floors.png\")\n   const TEX_WALLS  := preload(\"res://assets/tilesets/_Walls1.png\")\n\n2. Add a private helper method _make_tile_sprite after the constants/vars section:\n   func _make_tile_sprite(tex: Texture2D, col: int, row: int, gx: int, gy: int) -> Sprite2D:\n       var spr := Sprite2D.new()\n       spr.texture = tex\n       spr.region_enabled = true\n       spr.region_rect = Rect2(col * 32, row * 32, 32, 32)\n       spr.centered = false\n       spr.texture_filter = CanvasItem.TEXTURE_FILTER_NEAREST\n       spr.position = Vector2(gx * _tile_size, gy * _tile_size)\n       spr.scale = Vector2(float(_tile_size) / 32.0, float(_tile_size) / 32.0)\n       return spr\n\n3. Replace _generate_tiles() body entirely — remove all ColorRect creation. New body:\n   func _generate_tiles() -> void:\n       var tile_rng := RandomNumberGenerator.new()\n       for y in range(room_h):\n           for x in range(room_w):\n               var is_wall := (x == 0 or y == 0 or x == room_w - 1 or y == room_h - 1)\n               var spr: Sprite2D\n               if is_wall:\n                   spr = _make_tile_sprite(TEX_WALLS, 0, 0, x, y)\n               else:\n                   tile_rng.seed = x * 7919 + y * 6271 + room_w * 3\n                   var variant := tile_rng.randi_range(0, 3)\n                   spr = _make_tile_sprite(TEX_FLOORS, variant, 0, x, y)\n               tile_container.add_child(spr)\n\n4. Remove now-unused color constants WALL_COLOR and FLOOR_COLOR from the top of the file.\n   Keep PILLAR_COLOR, CRATE_COLOR, EXIT_COLOR — they are still used by _draw_obstacle and _draw_exit_tile (changed in story-2).",
      "priority": 1,
      "passes": true
    },
    {
      "id": "story-2",
      "title": "Obstacles and exits → Sprite2D",
      "description": "File: scripts/room_scene.gd\n\n1. Append two more texture constants to the existing const block (after TEX_FLOORS and TEX_WALLS from story-1):\n   const TEX_FURNITURE   := preload(\"res://assets/tilesets/_Meph_furniture.png\")\n   const TEX_DECORATIONS := preload(\"res://assets/tilesets/_Meph_decorations_2.png\")\n\n2. Replace _draw_obstacle() body. The existing signature is _draw_obstacle(pos: Vector2i, color: Color, symbol: String). Rename the color parameter to _color to suppress unused warning. New body:\n   func _draw_obstacle(pos: Vector2i, _color: Color, symbol: String) -> void:\n       var tex: Texture2D\n       var col: int\n       var row: int\n       match symbol:\n           \"P\":  tex = TEX_DECORATIONS; col = 0; row = 0\n           \"C\":  tex = TEX_FURNITURE;   col = 1; row = 0\n           \"L\":  tex = TEX_FURNITURE;   col = 2; row = 0\n           \"T\":  tex = TEX_FURNITURE;   col = 3; row = 0\n           _:    tex = TEX_FURNITURE;   col = 0; row = 0\n       var spr := _make_tile_sprite(tex, col, row, pos.x, pos.y)\n       tile_container.add_child(spr)\n       var lbl := Label.new()\n       lbl.text = symbol\n       lbl.position = Vector2(pos.x * _tile_size + 10, pos.y * _tile_size + 6)\n       overlay_container.add_child(lbl)\n\n3. Replace _draw_exit_tile() body — floor sprite with blue tint, keep the Label overlay:\n   func _draw_exit_tile(exit_pos: Vector2i, label_text: String) -> void:\n       var spr := _make_tile_sprite(TEX_FLOORS, 0, 0, exit_pos.x, exit_pos.y)\n       spr.modulate = Color(0.3, 0.6, 1.0, 1.0)\n       tile_container.add_child(spr)\n       var lbl := Label.new()\n       lbl.text = label_text\n       lbl.position = Vector2(exit_pos.x * _tile_size + 8, exit_pos.y * _tile_size + 6)\n       lbl.add_theme_color_override(\"font_color\", Color(1, 1, 1, 1))\n       overlay_container.add_child(lbl)\n\n4. Remove now-unused color constants PILLAR_COLOR, CRATE_COLOR, EXIT_COLOR from the top of the file.",
      "priority": 2,
      "passes": true
    },
    {
      "id": "story-3",
      "title": "Enemy sprites → Sprite2D",
      "description": "Files: scripts/room_scene.gd AND scripts/enemy.gd. Read both files fully before making any changes.\n\nPart A — room_scene.gd:_make_enemy_node():\n\n1. Append to the texture const block (after TEX_DECORATIONS from story-2):\n   const TEX_VARIETY := preload(\"res://assets/tilesets/_Meph_variety.png\")\n\n2. In _make_enemy_node(etype: String, pos: Vector2i), find the block that creates the ColorRect 'Sprite' child. Replace it with:\n   var ecol: int\n   match etype:\n       \"drone\":  ecol = 2\n       \"heavy\":  ecol = 4\n       \"elite\":  ecol = 6\n       _:        ecol = 0\n   var sprite := Sprite2D.new()\n   sprite.name = \"Sprite\"\n   sprite.texture = TEX_VARIETY\n   sprite.region_enabled = true\n   sprite.region_rect = Rect2(ecol * 32, 0, 32, 32)\n   sprite.centered = true\n   sprite.texture_filter = CanvasItem.TEXTURE_FILTER_NEAREST\n   sprite.position = Vector2(0, 0)\n   sprite.scale = Vector2(float(_tile_size) / 32.0, float(_tile_size) / 32.0)\n   Remove the old sprite.size, sprite.position offset, and sprite.color lines entirely.\n\nPart B — enemy.gd:_refresh_visuals():\n\n3. Read enemy.gd in full. Find _refresh_visuals(). If it contains any of these patterns:\n   - sprite.size = Vector2(tile_size - 4, tile_size - 4)\n   - sprite.position = Vector2(-tile_size / 2 + 2, -tile_size / 2 + 2)\n   Replace them with:\n   - sprite.scale = Vector2(float(tile_size) / 32.0, float(tile_size) / 32.0)\n   - Remove the sprite.position offset line (centered=true on Sprite2D handles centering, no offset needed)\n   If _refresh_visuals() does not touch sprite.size or sprite.position, no change is needed in enemy.gd.",
      "priority": 3,
      "passes": true
    },
    {
      "id": "story-4",
      "title": "Player sprite → Sprite2D",
      "description": "Files: scripts/game_scene.gd AND scripts/room_scene.gd. Read both files fully before making any changes.\n\nPart A — game_scene.gd:_setup_player():\n\n1. At the top of game_scene.gd (after 'extends Node2D'), add:\n   const TEX_VARIETY := preload(\"res://assets/tilesets/_Meph_variety.png\")\n\n2. In _setup_player(), find and replace the ColorRect sprite block:\n   REMOVE:\n       var sprite := ColorRect.new()\n       sprite.name = \"Sprite\"\n       sprite.size = Vector2(24, 24)\n       sprite.position = Vector2(-12, -12)\n       sprite.color = Color(0.0, 0.9, 0.3, 1.0)\n       _player.add_child(sprite)\n   REPLACE WITH:\n       var sprite := Sprite2D.new()\n       sprite.name = \"Sprite\"\n       sprite.texture = TEX_VARIETY\n       sprite.region_enabled = true\n       sprite.region_rect = Rect2(8 * 32, 0, 32, 32)\n       sprite.centered = true\n       sprite.texture_filter = CanvasItem.TEXTURE_FILTER_NEAREST\n       sprite.position = Vector2(0, 0)\n       _player.add_child(sprite)\n\nPart B — room_scene.gd:load_room():\n\n3. In room_scene.gd, in load_room(), find the line:\n       player.tile_size = _tile_size\n   Immediately after that line, add:\n       var pspr := player.get_node_or_null(\"Sprite\") as Sprite2D\n       if pspr:\n           pspr.scale = Vector2(float(_tile_size) / 32.0, float(_tile_size) / 32.0)\n   This ensures the player sprite scales correctly when entering rooms with different tile sizes.",
      "priority": 4,
      "passes": true
    }
  ]
}
